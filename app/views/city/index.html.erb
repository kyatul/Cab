<table class="table">
  <tr>
    <th>Name</th>
    <th></th>
  </tr>
<% @cities.each do |city| %>
  <tr>
    <td><%= city.name %></td>
  </tr>
<% end %>
<%= link_to 'Add City', admin_new_city_path %>
